export const metadata = {
    title: "Profile",
    description: "User Profile",
  };
  

export default function RootLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}